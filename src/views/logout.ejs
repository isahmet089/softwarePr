<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Çıkış Yapılıyor</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/logout.css">
</head>
<body>
    <div class="container">
        <div class="logo">
            <h1>Bilet Arama</h1>
        </div>
        
        <div class="logout-icon">👋</div>
        
        <div class="page-title">
            <h2>Çıkış Yapılıyor</h2>
            <p>Hesabınızdan güvenli bir şekilde çıkış yapılıyor</p>
        </div>
        
        <div class="loading">
            
        </div>
        
        <div class="message">Lütfen bekleyin, çıkış işleminiz gerçekleştiriliyor...</div>
        
        <div id="buttons" style="display: none;">
            <a href="/home" class="btn">Ana Sayfaya Dön</a>
            <a href="/login" class="btn btn-outline">Tekrar Giriş Yap</a>
        </div>
    </div>
    
   
</body>
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Simulate logout process
        setTimeout(function() {
            // Clear user data from localStorage
            localStorage.removeItem('isLoggedIn');
            localStorage.removeItem('userEmail');
            localStorage.removeItem('userName');
            localStorage.removeItem('userType');
            
            // Hide loading indicator
            document.querySelector('.loading').style.display = 'none';
            
            // Update message
            document.querySelector('.message').textContent = 'Çıkış işleminiz başarıyla tamamlandı.';
            
            // Show buttons
            document.getElementById('buttons').style.display = 'block';
        }, 2000); // 2 seconds delay for simulation
    });
</script>
</html>